colors = ('red', 'orange', 'yellow')
print(len(colors)) # 3

# access elements
print(colors[0]) # red

# error example
    # colors[0] = "RED"

# immutable
dict = {
    (1,2): 'home base'
}
print(dict) # perfectly fine
    # can't do the same with a list as a key
    