evens = {2, 4, 6, 8}
print(evens)
print(type(evens))

# can't put mutable structure inside
# test = {2, 4, 6, 8, True, 'asdf' [1,3,5]}
    # TypeError: unhashable type: 'list'

## set() function
# just empty curly braces {} indicates a DICTIONARY
evens = set()
print(evens)

# collection  of UNIQUE values
